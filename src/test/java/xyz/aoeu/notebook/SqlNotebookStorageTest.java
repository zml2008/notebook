package xyz.aoeu.notebook;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import java.util.UUID;

/**
 * Test notebook storage
 */
public class SqlNotebookStorageTest {
    /*@Test
    public void testUUIDToBytes() {
        for (int i = 0; i < 10; ++i) {
            UUID testId = UUID.randomUUID();
            byte[] bytes = SqlNotebookStorage.uuidToBytes(testId);
            UUID fromBytes = SqlNotebookStorage.bytesToUUID(bytes);
            assertEquals(testId, fromBytes);
        }

    }*/
}
